<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thiraa.foodlabs">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".MyCustomApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".dapur_location.LocationActivity"></activity>
        <activity android:name=".detail.AuthActivity"></activity>
        <activity android:name=".explore.BannerSampleActivity"></activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/map_key" />

        <activity android:name=".myprofile.ProfileActivity" />
        <activity android:name=".dapur_location.ReviewDapurActivity"></activity>

        <activity android:name=".dapur_location.ListDapurActivity" />
        <activity android:name=".home.HomeActivity"></activity>
        <activity android:name=".login_regis.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>  
        </activity>
        <activity android:name=".login_regis.RegisterActivity"></activity>

    </application>

</manifest>